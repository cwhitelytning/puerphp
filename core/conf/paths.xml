<?xml version="1.0" encoding="utf-8" ?>
<!-- This file is loaded automatically and is required to determine the core environment paths. -->
<paths>
    <!-- Root directory can be used by books. -->
    <path name="ROOT_DIR" value="{BOOK_DIR}/.."/>
    <!-- Library directory from which books are loaded. -->
    <path name="LIB_DIR" value="{ROOT_DIR}/lib"/>
</paths>